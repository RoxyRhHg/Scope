# Scope — 段永平式价值投资桌面筛选系统

基于段永平投资哲学（5大心智模型 + 8条决策启发式）的 A 股实时筛选和评分桌面应用。

底层投资逻辑详见 `shared/investment-philosophy.md`。

## 技术栈

| 层 | 技术 |
|----|------|
| 语言 | JavaScript (Node.js ES Module) + Python 3.9 + Rust |
| 前端 | 原生 HTML/CSS/JS (`public/`) |
| 后端 | Node.js 本地静态服务 (`src/server.js`) |
| 桌面壳 | Tauri 2.x (`src-tauri/`) |
| 测试 | 自建 harness (`tests/run-tests.js`) |
| 数据源 | 新浪分页行情 + BaoStock 行业映射 + 新浪热门概念 |

## 目录结构

```
Scope/
├── public/                  # 前端页面 (app.js, styles.css, index.html)
│   └── app.js               # 核心前端逻辑
├── src/
│   ├── server.js            # 本地 API 服务 (端口4173)
│   └── core/
│       ├── ranking.js       # v2 三角核心评分 + 共识调整 + 毛估估估值
│       ├── philosophyEngine.js  # 哲学层引擎（不为清单/能力圈/共识/估值）
│       ├── dashboard.js     # Dashboard 页面模型
│       ├── realDataAdapter.js  # 真实数据→内部模型适配（含管理质量代理评分）
│       ├── sampleData.js    # 样例数据（含 v2 派生维度）
│       ├── technicalIndicators.js  # MACD/BOLL/KDJ/成交量技术分析
│       └── textEncoding.js  # 文本编码处理
├── scripts/
│   ├── fetch_live_snapshot.py    # 真实 A 股快照抓取
│   └── fetch_stock_history.py    # 个股历史 K 线抓取
├── src-tauri/               # Tauri 桌面壳 (Cargo.toml + Rust 入口)
├── tests/
│   ├── ranking.test.js      # 评分/排序/过滤测试（v2 维度）
│   ├── philosophyEngine.test.js  # 哲学引擎专项测试
│   └── ...
└── shared/
    ├── investment-philosophy.md  # 底层投资逻辑完整文档
    └── project-context.md        # 本文档
```

## 常用命令

| 命令 | 说明 |
|------|------|
| `npm test` | 运行全部测试（34项） |
| `npm start` | 启动本地服务 → http://127.0.0.1:4173/ |
| `npm run tauri:dev` | Tauri 桌面开发模式 |
| `npm run tauri:build` | Tauri 桌面打包 |
| `npx tauri info` | 环境自检 |

## 评分体系（v2 段永平哲学融合版）

### 总分公式
```
总分 = 核心价值分 × 0.62 + 辅助主题分 × 0.18 + 资金适配分 × 0.12 + 共识调整分 × 0.08
```

### 核心价值分（权重重构）
| 维度 | 权重 | 哲学对应 |
|------|------|---------|
| 生意模式质量 | 0.32 | 对的生意（护城河/行业地位/差异化） |
| 管理与企业文化 | 0.22 | 对的人（诚信/股东回报/长期主义） |
| 估值与安全边际 | 0.18 | 对的价格（五档估值/毛估估区间） |
| 盈利质量 | 0.10 | 财务验证 |
| 现金流质量 | 0.08 | 财务验证 |
| 资产负债安全 | 0.06 | 不借钱原则 |
| 经营稳定性 | 0.04 | 财务验证 |

### 新增模块
- **不为清单硬过滤**：管理层诚信污点/商业模式硬伤/有息负债率>70%/审计非标/连续亏损
- **多视角共识机制**：基本面+估值+情绪+技术四维方向一致性 → 共识强度（高/中高/中等/低/极低）
- **毛估估估值体系**：五档估值结论（低估/合理偏低/合理/偏高估/明显高估）+ 机会成本比较
- **能力圈边界**：用户可设能力圈行业，圈外标记不列入重点关注

### 硬约束（不可突破）
1. 核心价值分 < 55 不进 Top 50
2. 核心价值分权重 ≥ 0.55
3. 生意模式质量权重 ≥ 0.22
4. 不为清单触发 → 直接排除
5. 概念热度 > 85 → 辅助分降权 50%

## 关键约束

- 真实数据优先：`/api/dashboard` 默认返回 live 模式快照，抓取失败自动回退 sample 模式
- 评分体系不可随意改：修改权重逻辑后必须跑 `npm test`
- 数据源链：新浪分页行情 → BaoStock 行业映射 → 新浪热门概念 Top 24
- 首次 live 数据拉取需 20-40 秒
- Tauri 骨架已建立但未完成首次桌面打包验证

## 当前状态

### 已完成
- v2 底层投资逻辑文档（`shared/investment-philosophy.md`）
- 哲学引擎实现（不为清单/能力圈/共识/毛估估估值）
- 三角核心评分重构（生意模式+管理质量+估值）
- 多视角共识机制
- 五档毛估估估值体系
- 不为清单硬过滤
- 全市场 Top 50、行业 Top 20、重点关注 1-2 只
- 技术指标分析（MACD/BOLL/KDJ/成交量/综合多空判断）
- 手动刷新 + 每小时自动刷新 + 权重/资金量调节
- 真实数据通路（新浪 + BaoStock）
- 本地 API 缓存 + live/sample 自动回退
- 全部 69 项测试通过
- **Obsidian 集成**：详情面板按标的主档案模板结构重组（Tab 切换），支持导出主档案/每日复盘 Markdown，支持写入 Obsidian Vault
- **概念体系升级**：从新浪动态 Top 24 切换为 11 个核心概念板块（AI/半导体/光通信/芯片/数据/算力/存储/能源/银行/具身智能/ARVR），通过硬编码映射+行业推断+名称关键词自动分配
- **股票搜索**：侧边栏搜索框，支持名称模糊和代码前缀过滤
- **自选股分组**：localStorage 持久化，支持创建/删除分组、添加/移除股票
- **ETF 列表**：47 只主要场内 ETF，按行业/宽基/跨境/债券/商品分类展示
- **涨停预测系统**：一年历史涨停数据（376只股/ETF，1804次事件）+ 五维评分预测引擎 + 一键每日预测

### 已知限制
- 管理质量评分目前为代理估算（基于市值/分红/PE），非真实违规记录
- 财务评分以轻量代理指标为主，未接入完整财报字段
- 未完成首次 Tauri 桌面打包验证
- Obsidian 导出的财务数据以代理值为主，标记了 `[代理]` 标识
- 概念映射为本地推断（非实时东方财富数据），精确度有限
- ETF 列表为硬编码主要品种，非全量实时数据

## 下一步建议

1. 接入管理层诚信数据库（证监会处罚记录/财务重述记录）
2. 补完整财报字段（ROE/ROIC/FCF/有息负债率真实值）
3. 完成首次 Tauri 桌面打包验证
4. 能力圈设置前端 UI
5. 网络可用时接入 AKShare 东方财富概念板块实时数据
6. ETF 接入 AKShare fund_etf_spot_em() 获取全量实时行情

## 当前任务队列

| 序号 | 任务 | 状态 | 负责人 |
|------|------|------|--------|
| T_v2.1 | 接入管理层诚信数据库 | ⬜ 待开始 | Codex |
| T_v2.2 | 补完整财报字段 | ⬜ 待开始 | Codex |
| T_v2.3 | 前端适配 v2 新字段 | ⬜ 待开始 | Codex |
| T_v2.4 | Tauri 桌面打包验证 | ⬜ 待开始 | Codex |
| T_v2.5 | 能力圈设置 UI | ⬜ 待开始 | Codex |

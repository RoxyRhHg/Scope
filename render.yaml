services:
  - type: web
    name: scope-a-share
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: "10000"
    plan: free
    region: singapore
    autoDeploy: true
